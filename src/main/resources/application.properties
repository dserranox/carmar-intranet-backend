spring.application.name=carmar-intranet-backend

# Ruta por defecto del Excel (puede sobreescribirse por query param ?path=)
carmar.excel.file=/mnt/data/ejemplo dani.xlsx

server.servlet.context-path=/carmar-backend/api

# DataSource
spring.datasource.url=${DBCP_CARMAR_URL:jdbc:postgresql://localhost:5432/carmardb}
spring.datasource.username=${DBCP_CARMAR_USERNAME:carmaradminuser}
spring.datasource.password=${DBCP_CARMAR_PASS:c4rm4rP4$}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Password mode (bcrypt por defecto)
security.password.mode=bcrypt
# JWT
security.jwt.secret=dev-0123456789abcdef-0123456789abcdef-0123456789abcdef
security.jwt.exp-minutes=120